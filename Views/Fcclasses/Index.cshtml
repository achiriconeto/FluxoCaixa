@model IEnumerable<FluxoCaixa03.Models.Fcclasse>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nm_classe)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nm_classe)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_classe">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_classe">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_classe">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
